<Controls:MetroWindow
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:Controls="clr-namespace:MahApps.Metro.Controls;assembly=MahApps.Metro"
    Title="MMC-If"
    Width="1200" Height="820"
    MinWidth="900" MinHeight="600"
    WindowStartupLocation="CenterScreen"
    TitleCharacterCasing="Normal"
    GlowBrush="{DynamicResource MahApps.Brushes.Accent}"
    NonActiveGlowBrush="{DynamicResource MahApps.Brushes.Accent}"
    BorderThickness="1"
    ShowIconOnTitleBar="False">

    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="pack://application:,,,/MahApps.Metro;component/Styles/Controls.xaml" />
                <ResourceDictionary Source="pack://application:,,,/MahApps.Metro;component/Styles/Fonts.xaml" />
                <ResourceDictionary Source="pack://application:,,,/MahApps.Metro;component/Styles/Themes/Dark.Steel.xaml" />
            </ResourceDictionary.MergedDictionaries>

            <!-- Sidebar button template: shade-lift on Pressed instead of the MahApps
                 Square default (which inverts colors via VSM and renders white text
                 invisible on the dark-theme sidebar). Foreground stays White;
                 #3A3A3A reads correctly under either theme during the brief press.
                 See reference_srl_wpf_brand.md S11. -->
            <ControlTemplate x:Key="SidebarButtonTemplate" TargetType="Button">
                <Border x:Name="Bd"
                        Background="{TemplateBinding Background}"
                        BorderBrush="{TemplateBinding BorderBrush}"
                        BorderThickness="{TemplateBinding BorderThickness}"
                        SnapsToDevicePixels="True">
                    <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                      VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                      Margin="{TemplateBinding Padding}"
                                      RecognizesAccessKey="True"
                                      SnapsToDevicePixels="True"/>
                </Border>
                <ControlTemplate.Triggers>
                    <Trigger Property="IsPressed" Value="True">
                        <Setter TargetName="Bd" Property="Background">
                            <Setter.Value>
                                <SolidColorBrush Color="#3A3A3A"/>
                            </Setter.Value>
                        </Setter>
                    </Trigger>
                    <Trigger Property="IsEnabled" Value="False">
                        <Setter TargetName="Bd" Property="Opacity" Value="0.5"/>
                    </Trigger>
                </ControlTemplate.Triggers>
            </ControlTemplate>

            <Style x:Key="ModuleButtonStyle" TargetType="Button" BasedOn="{StaticResource MahApps.Styles.Button.Square}">
                <Setter Property="Template" Value="{StaticResource SidebarButtonTemplate}"/>
                <Setter Property="Height" Value="42"/>
                <Setter Property="Margin" Value="8,3,8,3"/>
                <Setter Property="Padding" Value="12,0,12,0"/>
                <Setter Property="HorizontalContentAlignment" Value="Left"/>
                <Setter Property="FontSize" Value="13"/>
                <Setter Property="FontWeight" Value="Bold"/>
                <Setter Property="Controls:ControlsHelper.ContentCharacterCasing" Value="Normal"/>
                <Setter Property="Foreground" Value="White"/>
                <Setter Property="Background">
                    <Setter.Value>
                        <SolidColorBrush Color="#1E1E1E"/>
                    </Setter.Value>
                </Setter>
                <Setter Property="BorderBrush">
                    <Setter.Value>
                        <SolidColorBrush Color="#555555"/>
                    </Setter.Value>
                </Setter>
                <Setter Property="BorderThickness" Value="1"/>
            </Style>

            <Style x:Key="OptionsButtonStyle" TargetType="Button" BasedOn="{StaticResource MahApps.Styles.Button.Square}">
                <Setter Property="Template" Value="{StaticResource SidebarButtonTemplate}"/>
                <Setter Property="Height" Value="42"/>
                <Setter Property="Margin" Value="8,3,8,3"/>
                <Setter Property="Padding" Value="12,0,12,0"/>
                <Setter Property="HorizontalContentAlignment" Value="Left"/>
                <Setter Property="FontSize" Value="13"/>
                <Setter Property="FontWeight" Value="Bold"/>
                <Setter Property="Controls:ControlsHelper.ContentCharacterCasing" Value="Normal"/>
                <Setter Property="Foreground" Value="White"/>
                <Setter Property="Background">
                    <Setter.Value>
                        <SolidColorBrush Color="#1E1E1E"/>
                    </Setter.Value>
                </Setter>
                <Setter Property="BorderBrush">
                    <Setter.Value>
                        <SolidColorBrush Color="#555555"/>
                    </Setter.Value>
                </Setter>
                <Setter Property="BorderThickness" Value="1"/>
            </Style>

            <!-- Section header: Gray1 is theme-adaptive (#F9F9F9 dark, #333333 light);
                 Gray2 is static #7F7F7F and fails WCAG AA (3.83:1 / 4.00:1). -->
            <Style x:Key="SectionHeaderStyle" TargetType="TextBlock">
                <Setter Property="FontSize" Value="11"/>
                <Setter Property="FontWeight" Value="SemiBold"/>
                <Setter Property="Foreground" Value="{DynamicResource MahApps.Brushes.Gray1}"/>
                <Setter Property="Margin" Value="12,14,0,6"/>
            </Style>
        </ResourceDictionary>
    </Window.Resources>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="220"/>
            <ColumnDefinition Width="1"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- LEFT SIDEBAR -->
        <Border Grid.Column="0">
            <DockPanel>
                <StackPanel DockPanel.Dock="Top" Margin="14,14,14,0">
                    <TextBlock x:Name="txtAppTitle" Text="MMC-If" FontSize="20" FontWeight="Bold"/>
                    <TextBlock x:Name="txtAppVersion" Text="" FontSize="10"
                               Foreground="{DynamicResource MahApps.Brushes.Gray1}" Margin="0,2,0,0"/>
                </StackPanel>

                <StackPanel DockPanel.Dock="Bottom" Margin="14,0,14,14">
                    <Separator Margin="0,8,0,10"/>
                    <DockPanel LastChildFill="False">
                        <TextBlock x:Name="txtThemeLabel" Text="Dark Theme"
                                   DockPanel.Dock="Left" VerticalAlignment="Center" FontSize="12"/>
                        <Controls:ToggleSwitch x:Name="toggleTheme"
                                               DockPanel.Dock="Right"
                                               IsOn="True"
                                               OnContent="" OffContent=""
                                               Margin="0,0,0,0"
                                               MinWidth="0"/>
                    </DockPanel>
                </StackPanel>

                <ScrollViewer VerticalScrollBarVisibility="Auto" Margin="0,8,0,0">
                    <StackPanel>

                        <TextBlock Text="TOOLS" Style="{StaticResource SectionHeaderStyle}"/>
                        <Button Controls:ControlsHelper.ContentCharacterCasing="Normal" x:Name="btnRegistry"      Content="Registry"        Style="{StaticResource ModuleButtonStyle}" Tag="RegistryBrowser"/>
                        <Button Controls:ControlsHelper.ContentCharacterCasing="Normal" x:Name="btnEventLogs"     Content="Event Logs"      Style="{StaticResource ModuleButtonStyle}" Tag="EventLogViewer"/>
                        <Button Controls:ControlsHelper.ContentCharacterCasing="Normal" x:Name="btnDeviceInfo"    Content="Device Info"     Style="{StaticResource ModuleButtonStyle}" Tag="DeviceInfo"/>
                        <Button Controls:ControlsHelper.ContentCharacterCasing="Normal" x:Name="btnFiles"         Content="Files"           Style="{StaticResource ModuleButtonStyle}" Tag="FileExplorer"/>
                        <Button Controls:ControlsHelper.ContentCharacterCasing="Normal" x:Name="btnServices"      Content="Services"        Style="{StaticResource ModuleButtonStyle}" Tag="Services"/>
                        <Button Controls:ControlsHelper.ContentCharacterCasing="Normal" x:Name="btnCertificates"  Content="Certificates"    Style="{StaticResource ModuleButtonStyle}" Tag="CertificateStore"/>
                        <Button Controls:ControlsHelper.ContentCharacterCasing="Normal" x:Name="btnUsersGroups"   Content="Users &amp; Groups" Style="{StaticResource ModuleButtonStyle}" Tag="UsersGroups"/>

                        <TextBlock Text="NEW IN 1.0" Style="{StaticResource SectionHeaderStyle}"/>
                        <Button Controls:ControlsHelper.ContentCharacterCasing="Normal" x:Name="btnTaskScheduler" Content="Task Scheduler"  Style="{StaticResource ModuleButtonStyle}" Tag="TaskScheduler"/>
                        <Button Controls:ControlsHelper.ContentCharacterCasing="Normal" x:Name="btnNetworking"    Content="Networking"      Style="{StaticResource ModuleButtonStyle}" Tag="Networking"/>
                        <Button Controls:ControlsHelper.ContentCharacterCasing="Normal" x:Name="btnDisks"         Content="Disks"           Style="{StaticResource ModuleButtonStyle}" Tag="Disks"/>
                        <Button Controls:ControlsHelper.ContentCharacterCasing="Normal" x:Name="btnGroupPolicy"   Content="Group Policy"    Style="{StaticResource ModuleButtonStyle}" Tag="GroupPolicy"/>

                    </StackPanel>
                </ScrollViewer>

            </DockPanel>
        </Border>

        <!-- Sidebar / content separator -->
        <Border Grid.Column="1" Background="{DynamicResource MahApps.Brushes.Gray8}"/>

        <!-- MAIN CONTENT AREA -->
        <Grid Grid.Column="2">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="140"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Module header -->
            <Border Grid.Row="0" Padding="16,12,16,10"
                    Background="{DynamicResource MahApps.Brushes.Gray10}">
                <StackPanel>
                    <TextBlock x:Name="txtModuleTitle" Text="Welcome" FontSize="18" FontWeight="SemiBold"/>
                    <TextBlock x:Name="txtModuleSubtitle" Text="Select a tool from the sidebar to begin."
                               FontSize="11"
                               Foreground="{DynamicResource MahApps.Brushes.Gray1}"
                               Margin="0,2,0,0"/>
                </StackPanel>
            </Border>

            <!-- Module content host -->
            <ContentControl x:Name="contentHost" Grid.Row="1" Margin="8,8,8,0"/>

            <!-- Resizable splitter -->
            <GridSplitter Grid.Row="2" Height="5" HorizontalAlignment="Stretch"
                          Background="{DynamicResource MahApps.Brushes.Gray8}"
                          ResizeDirection="Rows"/>

            <!-- Log output drawer -->
            <Border Grid.Row="3">
                <DockPanel>
                    <TextBlock x:Name="lblLogOutput" DockPanel.Dock="Top" Text="LOG OUTPUT"
                               FontSize="10" FontWeight="SemiBold"
                               Margin="8,6,0,4"/>
                    <TextBox x:Name="txtLog"
                             IsReadOnly="True"
                             TextWrapping="NoWrap"
                             VerticalScrollBarVisibility="Auto"
                             HorizontalScrollBarVisibility="Auto"
                             FontFamily="Cascadia Code, Consolas, Courier New"
                             FontSize="11"
                             BorderThickness="0"
                             Margin="4,0,4,4"/>
                </DockPanel>
            </Border>

            <!-- Status bar -->
            <Border Grid.Row="4" Padding="8,4,8,4"
                    Background="{DynamicResource MahApps.Brushes.Gray10}">
                <TextBlock x:Name="txtStatus" Text="Ready." FontSize="11"/>
            </Border>
        </Grid>
    </Grid>

</Controls:MetroWindow>
