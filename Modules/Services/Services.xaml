<UserControl
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:Controls="clr-namespace:MahApps.Metro.Controls;assembly=MahApps.Metro">

    <DockPanel>
        <!-- Toolbar -->
        <Border DockPanel.Dock="Top" Padding="8,6,8,6"
                Background="{DynamicResource MahApps.Brushes.Gray10}">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="280"/>
                    <ColumnDefinition Width="8"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="6"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="6"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="6"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <TextBox x:Name="txtFilter" Grid.Column="0"
                         VerticalAlignment="Center" VerticalContentAlignment="Center"
                         Controls:TextBoxHelper.Watermark="Filter services by name, display name, or description..."/>
                <Button Controls:ControlsHelper.ContentCharacterCasing="Normal" x:Name="btnRefresh" Grid.Column="2" Content="Refresh"
                        MinWidth="80" Padding="8,4,8,4"
                        Style="{DynamicResource MahApps.Styles.Button.Square.Accent}"/>
                <Button Controls:ControlsHelper.ContentCharacterCasing="Normal" x:Name="btnStart" Grid.Column="4" Content="Start"
                        MinWidth="70" Padding="8,4,8,4" Visibility="Collapsed"
                        Style="{DynamicResource MahApps.Styles.Button.Square}"/>
                <Button Controls:ControlsHelper.ContentCharacterCasing="Normal" x:Name="btnStop" Grid.Column="6" Content="Stop"
                        MinWidth="70" Padding="8,4,8,4" Visibility="Collapsed"
                        Style="{DynamicResource MahApps.Styles.Button.Square}"/>
                <Button Controls:ControlsHelper.ContentCharacterCasing="Normal" x:Name="btnRestart" Grid.Column="8" Content="Restart"
                        MinWidth="80" Padding="8,4,8,4" Visibility="Collapsed"
                        Style="{DynamicResource MahApps.Styles.Button.Square}"/>
            </Grid>
        </Border>

        <!-- Detail pane bottom -->
        <Border DockPanel.Dock="Bottom" Height="160"
                BorderBrush="{DynamicResource MahApps.Brushes.Gray8}"
                BorderThickness="0,1,0,0">
            <DockPanel>
                <TextBlock DockPanel.Dock="Top" Text="SERVICE DETAIL"
                           FontSize="10" FontWeight="SemiBold"
                           Foreground="{DynamicResource MahApps.Brushes.Gray3}"
                           Margin="8,6,0,4"/>
                <TextBox x:Name="txtDetail"
                         IsReadOnly="True" TextWrapping="Wrap"
                         VerticalScrollBarVisibility="Auto"
                         FontFamily="Cascadia Code, Consolas, Courier New"
                         FontSize="11" BorderThickness="0"
                         Margin="4,0,4,4"/>
            </DockPanel>
        </Border>

        <DataGrid x:Name="gridSvc"
                  AutoGenerateColumns="False"
                  CanUserAddRows="False"
                  CanUserDeleteRows="False"
                  IsReadOnly="True"
                  SelectionMode="Single"
                  SelectionUnit="FullRow"
                  GridLinesVisibility="Horizontal"
                  HeadersVisibility="Column"
                  RowHeaderWidth="0"
                  BorderThickness="0"
                  ColumnHeaderHeight="30">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Name"         Width="180" Binding="{Binding Name}"/>
                <DataGridTextColumn Header="Display Name" Width="260" Binding="{Binding DisplayName}"/>
                <DataGridTextColumn Header="Status"       Width="90"  Binding="{Binding Status}"/>
                <DataGridTextColumn Header="Startup"      Width="110" Binding="{Binding StartupType}"/>
                <DataGridTextColumn Header="Account"      Width="170" Binding="{Binding Account}"/>
                <DataGridTextColumn Header="Description"  Width="*"   Binding="{Binding Description}"/>
            </DataGrid.Columns>
            <DataGrid.ContextMenu>
                <ContextMenu>
                    <MenuItem x:Name="mnuCopyName"    Header="Copy Service Name"/>
                    <MenuItem x:Name="mnuCopyDisplay" Header="Copy Display Name"/>
                    <MenuItem x:Name="mnuCopyPath"    Header="Copy Path"/>
                </ContextMenu>
            </DataGrid.ContextMenu>
        </DataGrid>
    </DockPanel>
</UserControl>
